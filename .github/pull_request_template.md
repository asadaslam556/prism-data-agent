## What this changes

## Why

## Checks
- [ ] `ruff check app tests list_models.py` and `python -m pytest` pass in `backend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] Guardrail changes come with tests
- [ ] CHANGELOG.md updated for anything user-visible
