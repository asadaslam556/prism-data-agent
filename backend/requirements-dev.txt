# Everything in requirements.txt, plus the tooling CI runs.
-r requirements.txt

pytest>=8.0                    # the model is mocked in every test
ruff>=0.5                      # linter; config lives in pyproject.toml