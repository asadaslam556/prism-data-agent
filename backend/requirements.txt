# Everything the app imports at runtime, plus both LLM providers.
# Python 3.11+.

# --- API ---
fastapi>=0.141.1
uvicorn[standard]>=0.53.0
python-multipart>=0.0.32       # multipart parsing for CSV upload
starlette>=1.6.0               # pulled in by fastapi; pinned so SSE behaviour is predictable

# --- Data ---
pandas>=3.0.5
numpy>=2.4.6
matplotlib>=3.11.2             # charts, rendered headless via the Agg backend
sqlalchemy>=2.0.54
sqlparse>=0.6.0                # used by the SQL guardrail to check statements

# --- Agent / LLM ---
langgraph==1.2.11              # the agent graph itself
langchain-core==1.6.3          # message types shared by every provider
langchain-ollama>=1.1.0        # LLM_PROVIDER=ollama  (local, default)
langchain-openai==1.6.2        # LLM_PROVIDER=openai  (OpenAI + any compatible endpoint)

# --- Config ---
pydantic>=2.13.5
pydantic-settings>=2.15.0
python-dotenv>=1.2.3           # lets pydantic-settings read backend/.env

# --- HTTP ---
httpx>=0.28.1                  # transport under the provider SDKs

# --- Optional database drivers -------------------------------------------
# CSV upload and SQLite files work with no extra install. For anything else,
# uncomment the driver you need. psycopg2-binary is the wheel build; use plain
# psycopg2 only if you have the Postgres build tools set up.
# psycopg2-binary>=2.9         # PostgreSQL
# pymysql>=1.1                 # MySQL / MariaDB
# pyodbc>=5.1                  # SQL Server
# cx_Oracle>=8.3               # Oracle
