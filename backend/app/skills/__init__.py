"""The agent's capabilities, one module each.

A skill exposes NAME, DESCRIPTION and run(...) and returns a SkillResult. To add
one: drop a module in here, give it a node in agent/graph.py, and mention the
action in the planner prompt.
"""
