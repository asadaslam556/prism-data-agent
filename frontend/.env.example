# Base URL of the backend API. Leave empty when using the Vite dev proxy or the
# Docker/nginx setup (both forward /api). Set it only for a split deployment,
# e.g. VITE_API_BASE=https://my-backend.example.com
VITE_API_BASE=
